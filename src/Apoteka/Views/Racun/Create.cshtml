@model Apoteka.Models.Racun

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Racun</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="OsiguranikId" class="control-label">JMBG osiguranika</label>
                <select asp-for="OsiguranikId" class ="form-control" asp-items="ViewBag.OsiguranikId"></select>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Kreiraj novi racun" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
